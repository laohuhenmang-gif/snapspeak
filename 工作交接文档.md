# 工作交接文档

> 交接日期：2026-06-09
> Git 用户：snapspeak <snapspeak@users.noreply.github.com>

---

## 项目总览

本仓库包含 **2 个项目** + **1 个模板文件**，均围绕"任务管理/工作备忘"主题：

| 项目 | 类型 | 技术栈 | 版本 |
|------|------|--------|------|
| **reminder-app** (工作备忘) | 桌面应用 | Tauri v2 + Vanilla JS + Rust | 1.0.0 |
| **SnapSpeak** (语拍提醒) | 移动应用 | Expo SDK 56 + React Native + TypeScript | 1.0.0 |
| 工作备忘模板.json | 模板文件 | JSON | - |

---

## 一、reminder-app — 桌面工作备忘应用

### 1.1 简介
基于 Tauri v2 的桌面任务提醒应用，支持任务的增删改查、分类管理、周视图日历、定期任务和桌面原生通知。

### 1.2 技术栈

| 层 | 技术 |
|----|------|
| 桌面框架 | Tauri v2 (Rust) |
| 前端 | Vanilla JavaScript (ES Modules) |
| 打包工具 | Vite v5 |
| 数据存储 | JSON 文件（本地文件系统） |
| 通知 | Tauri Plugin Notification（系统原生通知） |
| 样式 | 自定义 CSS（CSS 变量主题系统） |

### 1.3 项目路径

```
F:\工作备忘\reminder-app\
```

### 1.4 目录结构

```
reminder-app/
├── index.html              # 入口 HTML（中文界面）
├── package.json            # NPM 配置
├── vite.config.js          # Vite 配置（端口 1420）
├── src/
│   ├── main.js             # 主入口：路由初始化、视图渲染
│   ├── style.css           # 全部样式（~390行）
│   └── js/
│       ├── api.js          # Tauri 调用封装（13个命令）
│       ├── components.js   # UI 组件（任务卡片、弹窗、Toast）
│       ├── notification.js # 系统通知监听
│       ├── router.js       # Hash 路由（#today/#weekly/#tasks/#settings）
│       ├── task-form.js    # 任务表单（新增/编辑）
│       └── weekly-grid.js  # 周视图日历
├── data/
│   ├── tasks.json          # 示例任务数据
│   └── categories.json     # 示例分类数据
├── dist/                   # 前端构建产物
├── src-tauri/
│   ├── tauri.conf.json     # Tauri 配置
│   ├── Cargo.toml          # Rust 依赖
│   └── src/
│       ├── main.rs         # 程序入口
│       ├── lib.rs          # Tauri 应用初始化 + 12个命令注册
│       ├── error.rs        # 错误类型定义
│       ├── models/         # 数据模型（Task, Category）
│       ├── commands/       # Tauri 命令实现
│       ├── storage/        # JSON 文件存储层
│       └── notification/   # 提醒调度器（每30秒轮询）
└── node_modules/           # 已安装
```

### 1.5 核心功能

- **4个视图页面**：今日待办（#today）、周视图（#weekly）、全部任务（#tasks）、设置（#settings）
- **任务 CRUD**：标题、描述、时间、优先级（高/中/低）、分类、标签、重复类型
- **分类管理**：新增/编辑/删除分类，颜色标注
- **搜索与过滤**：全文搜索，按优先级和状态过滤
- **周视图日历**：周一至周日7列网格布局
- **定期任务**：支持每天/每周/每月重复（数据模型已支持，自动推进逻辑待完善）
- **桌面通知**：后台 Rust 线程每30秒轮询，临近任务触发系统通知
- **Hash 路由**：前端 SPA 路由

### 1.6 运行方式

```bash
cd F:\工作备忘\reminder-app

# 开发模式（需要 Rust 环境）
npm run tauri dev

# 仅前端开发
npm run dev          # 启动 Vite 开发服务器 (localhost:1420)

# 构建
npm run build        # 构建前端
npm run tauri build  # 构建桌面安装包
```

### 1.7 前端依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| `@tauri-apps/api` | ^2.0.0 | Tauri JS 桥接 |
| `@tauri-apps/plugin-notification` | ^2.0.0 | 系统通知 |

### 1.8 Rust 依赖 (Cargo.toml)

| Crate | 用途 |
|-------|------|
| `tauri` 2 | 应用框架 |
| `tauri-plugin-notification` 2 | 通知插件 |
| `serde` / `serde_json` | JSON 序列化 |
| `chrono` | 日期时间处理 |
| `uuid` | 任务/分类 ID 生成 |

### 1.9 当前状态

- 前端和 Rust 后端均已编译过（dist/ 和 target/ 存在）
- 种子数据已准备（data/ 目录）
- 无 README 文档
- **待完善**：定期任务的自动推进逻辑（创建下一个重复实例）

### 1.10 生产就绪改进（v1.0.0）

- **时区修复** — datetime-local 不再错误转换为 UTC，保持本地时间
- **定期任务自动推进** — 完成任务后自动计算下一个周期
- **安全增强** — CSP 已启用，XSS 防护，输入验证
- **错误处理** — 结构化 AppError 替代 String，错误日志输出
- **数据导出** — 设置页新增 JSON 导出功能（需 dialog 插件权限）
- **写入保护** — 保存失败时输出错误日志，不再静默丢数据
- **通知优化** — 引入 `(id, datetime)` 双键跟踪，定期任务可重复提醒

### 1.11 关键文件说明

| 文件 | 说明 |
|------|------|
| `src-tauri/tauri.conf.json` | 窗口 1024x720，产品名"工作备忘"，标识符 com.workmemo.app |
| `src-tauri/capabilities/default.json` | 权限配置：core:default, notification:default, dialog:default |
| `src-tauri/src/storage/json_storage.rs` | 数据存储核心，Mutex + JSON 文件读写 |
| `src-tauri/src/notification/scheduler.rs` | 后台提醒线程，30秒轮询 |
| `src/js/api.js` | 13个 Tauri 命令的 JS 封装 |

---

## 二、SnapSpeak (语拍提醒 · AI助理) — 移动 AI 任务助理

### 2.1 简介
基于 React Native (Expo SDK 56) 的移动端 AI 智能任务管理助手。用户可通过**文字输入**、**语音**或**拍照**（OCR）与 AI 对话，AI 自动识别意图并完成任务的创建、编辑、完成、提醒等操作。

### 2.2 技术栈

| 层 | 技术 |
|----|------|
| 框架 | React Native 0.85.3 + Expo SDK 56 |
| 语言 | TypeScript 6.0（strict） |
| 路由 | Expo Router（文件路由） |
| 本地存储 | SQLite（expo-sqlite） |
| AI 后端 | DeepSeek API（主要）/ OpenAI 兼容 / 自定义 |
| 语音输入 | expo-speech-recognition（实时 STT） |
| 语音输出 | expo-speech（TTS） |
| 通知 | expo-notifications（本地定时提醒） |
| OCR | DeepSeek Vision 模型 |
| 安全存储 | expo-secure-store（API Key） |
| 拍照/选图 | expo-camera / expo-image-picker |
| 构建部署 | EAS（Expo Application Services） |

### 2.3 项目路径

```
F:\工作备忘\SnapSpeak\
```

### 2.4 目录结构

```
SnapSpeak/
├── app/                          # Expo Router 页面
│   ├── _layout.tsx               # 根布局（引导检查、主题、通知初始化）
│   ├── onboarding.tsx            # 3页引导向导
│   ├── knowledge.tsx             # AI 知识库管理
│   └── (tabs)/
│       ├── _layout.tsx           # 底部标签栏（3个标签）
│       ├── index.tsx             # 主界面 - 对话式 AI 交互（1376行！）
│       ├── tasks.tsx             # 全部任务列表
│       └── settings.tsx          # 设置页（AI配置、主题、数据管理）
├── components/
│   ├── ChatBubble.tsx            # 聊天气泡（文本/图片/卡片/错误）
│   ├── InputBar.tsx              # 底部输入栏（文字/语音/相机，PanResponder）
│   ├── PixelAvatar.tsx           # 16x16 像素风头像
│   ├── TaskCard.tsx              # 任务卡片（复选框+标签）
│   └── Toast.tsx                 # 动画 Toast
├── services/
│   ├── ai.ts                     # AI 核心调用（chat, parseTask, editTask, OCR等）
│   ├── ai-config.ts              # API Key/模型/提供商管理
│   ├── ai-types.ts              # AI 类型定义 + 系统提示词（165行）
│   ├── http-client.ts           # HTTP 请求封装（超时+重试）
│   ├── network.ts               # 网络状态监听
│   ├── notification.ts          # 通知创建/调度/处理
│   ├── ocr.ts                   # OCR 服务（DeepSeek Vision）
│   ├── reminder-service.ts      # 提醒服务（通知监听+TTS播报）
│   ├── speech.ts                # 语音识别（STT）+ 语音合成（TTS）
│   ├── storage.ts              # SQLite 数据库层（8张表）
│   ├── theme-config.ts         # 主题持久化
│   └── theme-context.tsx       # 主题 React Context
├── constants/
│   ├── index.ts                 # 颜色、分类、优先级、模型列表
│   ├── interaction.ts           # 交互状态机定义
│   └── themes.ts               # 6套主题定义
├── types/
│   └── index.ts                 # TypeScript 类型定义
├── scripts/
│   └── generate-icons.mjs      # Jimp 像素图标生成脚本
├── assets/                      # 图标资源
├── android/                     # Android 原生项目
├── eas.json                     # EAS 构建配置
├── app.json                     # Expo 配置
├── tsconfig.json                # TypeScript 配置
├── snapspeak-v0.5.0.apk         # v0.5.0 APK
└── SnapSpeak-v1.1.0.apk         # v1.1.0 APK
```

### 2.5 核心功能

1. **多模态对话交互** — 文字/语音/拍照三种输入方式
2. **自然语言任务管理** — AI 解析中英文自然语言，自动创建/编辑/完成/删除任务
3. **OCR 识别** — 拍照识别白板/手写笔记中的待办事项
4. **智能提醒** — 根据任务时间自动设置本地通知，支持延后/重排/关闭
5. **每日简报** — 早上自动生成当天任务摘要
6. **每日回顾** — 18:00 后 AI 自动生成完成率、障碍物分析和建议
7. **过期提醒** — 主动检测过期任务并询问原因
8. **知识库** — AI 学习用户偏好/习惯/障碍/规则，可在"知识库"页面查看管理
9. **确认机制** — AI 操作需用户显式确认（"确认"/"好的"/"可以"）后才执行
10. **6套主题** — 像素风、暗黑、报纸、网格、绿屏、琥珀

### 2.6 交互状态机

```
idle → listening → transcribing → sending → thinking → action_pending → executing → success | error
```
每步有15秒超时保护，防止卡死。

### 2.7 SQLite 数据库（8张表）

| 表名 | 用途 |
|------|------|
| `tasks` | 核心任务数据 |
| `captures` | 用户输入记录（语音/图片/文本） |
| `reminders` | 提醒记录 |
| `ai_actions` | AI 操作日志 |
| `memories` | AI 知识库（偏好/障碍/规则/习惯） |
| `reflections` | 每日/每周回顾记录 |
| `blockers` | 任务阻碍/扣分记录 |
| `conversations` | 对话历史 |

### 2.8 AI Provider 配置

- **供应商**：DeepSeek / OpenAI兼容 / 自定义
- **模型**：DeepSeek Chat, DeepSeek Reasoner, DeepSeek V4 Flash, DeepSeek V4 Pro / GPT-4o, GPT-4o-mini 等
- **API Key**：存储在 expo-secure-store（优先），回退至 AsyncStorage
- **系统提示词**：165行中文 AI 角色设定，定义语调（友好温暖、不说教）、能力范围、确认卡片机制

### 2.9 运行方式

```bash
cd F:\工作备忘\SnapSpeak

# 安装依赖
npm install

# 启动 Expo 开发服务器
npm start           # 或 npx expo start

# Android 真机/模拟器
npm run android     # 或 npx expo run:android

# Web 预览
npm run web         # 或 npx expo start --web
```

### 2.10 构建 APK

```bash
# EAS 构建
npx eas build --platform android --profile preview   # 预览版
npx eas build --platform android --profile production # 正式版
```

EAS 项目 ID：`c1da7284-e3e6-46e8-88bb-fcb26421fd40`（owner: `laohu`）

### 2.11 Android 配置

| 配置项 | 值 |
|--------|-----|
| 包名 | `com.snapspeak.app` |
| 最低 SDK | 24 (Android 7.0) |
| 版本名 | 1.0.0 |
| 权限 | CAMERA, RECORD_AUDIO, POST_NOTIFICATIONS, SCHEDULE_EXACT_ALARM, READ_EXTERNAL_STORAGE, READ_MEDIA_IMAGES |

### 2.12 生产就绪改进（v1.0.0）

- **版本统一** — package.json 和 app.json 统一为 1.0.0
- **语音识别修复** — 修复 `_manualStop` 永不重置导致的永久损坏、recognizer 覆盖泄漏
- **导入/导出安全** — importAllData 包装事务，SQL 注入防护（列名校验白名单），exportAllData 原子读取
- **监听器清理** — AppState 监听、通知监听、ReminderService 均具备卸载清理
- **Error Boundary** — 全局错误边界，捕获未预期崩溃
- **隐私政策** — 新增隐私政策页面（`app/privacy.tsx` + PRIVACY.md）
- **README** — 两个项目均添加完整 README.md
- **每日复盘修复** — plannedCount / overdueCount 统计逻辑修正
- **updateTask 修复** — 允许清空 datetime 字段
- **AI JSON 解析** — 支持 AI 返回 markdown 代码块包裹的 JSON
- **Provider 验证** — getProvider 返回值强制校验，防止非法值导致崩溃

### 2.13 当前状态

- 版本已统一为 1.0.0
- 已有两个 APK 构建产物（v0.5.0 和 v1.1.0），需重新打包 v1.0.0
- 无 README 文档（已补全）
- **注意**：主界面 `app/(tabs)/index.tsx` 达 1376 行，建议后续拆分解耦

### 2.13 关键文件说明

| 文件 | 说明 |
|------|------|
| `app/(tabs)/index.tsx` | 核心界面，对话 AI、任务 CRUD、OCR、每日回顾等（1376行） |
| `services/ai.ts` | AI API 调用封装 |
| `services/ai-types.ts` | AI 类型定义 + 165行系统提示词 |
| `services/storage.ts` | SQLite 数据库操作层 |
| `services/http-client.ts` | HTTP 请求封装（30秒超时、2次重试+指数退避） |
| `components/InputBar.tsx` | 输入栏（长按录音、上滑取消，类似微信语音） |
| `constants/themes.ts` | 6套主题色彩定义 |
| `constants/interaction.ts` | 交互状态机 |
| `eas.json` | EAS 构建配置（preview + production） |

---

## 三、工作备忘模板.json

### 3.1 说明
JSON 格式的个人工作安排模板，用于记录每周日程、定期任务、项目里程碑和待办事项。

### 3.2 结构

```json
{
  "姓名": "",
  "每周工作安排": { "周一"~"周日": [{ "时间", "事项", "提醒", "提前分钟" }] },
  "定期任务": [{ "名称", "频率", "日期", "时间", "提醒" }],
  "项目里程碑": [{ "项目名", "里程碑", "截止日期", "优先级" }],
  "一次性待办": []
}
```

---

## 四、开发环境要求

### reminder-app
- Node.js >= 18
- Rust toolchain (rustup)
- Windows: Visual Studio Build Tools + Windows SDK 或 WebView2

### SnapSpeak
- Node.js >= 18
- Expo CLI (`npx expo`)
- Android Studio（用于模拟器/构建）
- EAS CLI（用于云端构建）

---

## 五、注意事项

1. ~~**reminder-app** 的定期任务自动推进逻辑尚未完整实现~~ ✅ 已修复
2. **SnapSpeak** 的 `app/(tabs)/index.tsx` 代码量过大（1376行），建议后续拆分到独立模块
3. ~~**SnapSpeak** 的版本号不一致~~ ✅ 已统一为 1.0.0
4. 两个项目的 Git 仓库均未初始化提交，需确认后推送
5. SnapSpeak 的 API Key 存储在设备安全存储中，交接后需重新配置
6. SnapSpeak 的 EAS 构建需要在 Expo 账号 `laohu` 下操作
