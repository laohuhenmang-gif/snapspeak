# 语拍提醒 / SnapSpeak — 产品说明书

---

## 目录

1. [产品定位](#1-产品定位)
2. [技术架构](#2-技术架构)
3. [文件结构](#3-文件结构)
4. [UI 设计 — 黑白像素艺术风格](#4-ui-设计--黑白像素艺术风格)
5. [用户交互流程](#5-用户交互流程)
6. [API 服务详解](#6-api-服务详解)
7. [数据模型](#7-数据模型)
8. [构建与部署](#8-构建与部署)
9. [主题系统](#9-主题系统)
10. [开发指南](#10-开发指南)
11. [版本历史](#11-版本历史)

---

## 1. 产品定位

**语拍提醒（SnapSpeak）** 是一款 AI 驱动的任务管理机器人。用户通过自然语言（文字或语音）与 AI 对话来创建、修改、完成、删除、查询任务，无需填写表单。

**核心价值：** 对机器人说一句话，搞定一切任务操作。

### 适用场景

| 场景 | 交互方式 |
|------|----------|
| 快速记任务 | "明天下午三点开会" → AI 解析时间/标题/优先级 → 弹出确认卡片 |
| 批量操作 | "完成今天所有任务" → AI 识别意图 → 批量标记 |
| 修改任务 | 点任务 → 浮出对话面板 → "改到后天上午" → 自动调整 |
| 查询状态 | "还有几个没完成" → AI 返回统计 |
| 拍照录入 | 拍备忘录/白板照片 → OCR 识别 → 提取任务列表 |
| 提醒处理 | 弹出提醒 → TTS 语音播报 → "推迟1小时" → AI 处理 |

---

## 2. 技术架构

### 整体架构

```
[React Native (Expo SDK 56)]
  ├── UI Layer (app/ + components/)
  │   ├── expo-router (文件路由)
  │   ├── Bottom Sheet 对话面板 (ChatSheet)
  │   └── 12 套主题系统 (ThemeContext + AsyncStorage)
  ├── Services Layer (services/)
  │   ├── ai.ts ──── DeepSeek API 调用 (8 个 AI 函数)
  │   ├── storage.ts ─ AsyncStorage 持久化 (CRUD)
  │   ├── notification.ts ─ 系统通知 日程提醒
  │   ├── speech.ts ─ TTS 播报 + expo-audio 音频
  │   └── ocr.ts ─── DeepSeek Vision OCR
  └── Native Modules
      ├── expo-audio ──────── 提醒音频播放
      ├── expo-blur ───────── 毛玻璃效果
      ├── expo-camera + image-picker ─ 拍照/相册
      ├── expo-haptics ────── 触感振动反馈
      ├── expo-notifications ─ 系统通知提醒
      ├── expo-speech ─────── TTS 语音合成
      └── expo-build-properties ─ Android SDK 版本控制
```

### 核心依赖

| 层 | 依赖 | 版本 |
|----|------|------|
| 前端框架 | `expo` | ~56.0.9 |
| 前端框架 | `react-native` | 0.85.3 |
| 路由 | `expo-router` | ~56.2.9 |
| AI | DeepSeek API (HTTPS) | deepseek-v4-flash, deepseek-chat |
| 存储 | `@react-native-async-storage/async-storage` | 2.2.0 |
| 音频 | `expo-audio` | ~56.0.11 |
| 音频 | `expo-speech` | ~56.0.3 |
| 输入 | `expo-camera` | ~56.0.7 |
| 输入 | `expo-image-picker` | ~56.0.16 |
| 交互 | `expo-haptics` | ~56.0.3 |
| 交互 | `expo-blur` | ~56.0.4 |
| 交互 | `@expo/vector-icons` | ^15.0.2 |
| 通知 | `expo-notifications` | ~56.0.16 |
| 主题 | `expo-system-ui` | ~56.0.3 |
| 构建 | EAS Build (preview profile) | APK 输出 |

### 路由结构

```
Route Map (expo-router file-based routing)
──────────────────────────────────────────
app/
├── _layout.tsx ──── Root Layout
│   ├── (tabs)/
│   │   ├── _layout.tsx ── Tab Navigator
│   │   ├── index.tsx ──── 首页 "今日任务"
│   │   ├── tasks.tsx ──── 全部任务 (待办/已完成)
│   │   └── settings.tsx ─ 设置 (API Key, 主题, 性能)
│   ├── new-task/
│   │   ├── index.tsx ──── 多方式入口
│   │   ├── camera.tsx ─── 拍照OCR
│   │   ├── voice.tsx ──── 语音输入
│   │   └── confirm.tsx ── 确认编辑
│   ├── task-edit/[id].tsx ── 任务详情编辑
│   ├── reminder-popup.tsx ── 提醒弹窗
│   └── chat.tsx ──── 旧聊天页 (已弃用，保留备用)
```

---

## 3. 文件结构

```
SnapSpeak/
├── app/                          # 路由页面 (expo-router)
│   ├── _layout.tsx               # 根布局 + ErrorBoundary + ThemeProvider
│   ├── (tabs)/
│   │   ├── _layout.tsx           # Tab 栏配置 (Feather 图标 + 像素边框)
│   │   ├── index.tsx             # 首页 (今日任务 + 机器人输入 + multiIntent)
│   │   ├── tasks.tsx             # 全部任务 + 待办/已完成分段 + 分类筛选
│   │   └── settings.tsx          # 设置 (API Key, 模型, 主题, 暗色, 性能)
│   ├── chat.tsx                  # 旧独立聊天页 (已弃用，保留备用)
│   ├── new-task/
│   │   ├── _layout.tsx
│   │   ├── index.tsx             # 多方式入口 (语音/拍照/手动)
│   │   ├── camera.tsx            # 拍照/相册 + DeepSeek Vision OCR
│   │   ├── voice.tsx             # 语音输入 (mock STT)
│   │   └── confirm.tsx           # 手动确认/编辑任务表单
│   ├── task-edit/
│   │   └── [id].tsx              # 任务编辑页 + AI 快捷修改 (InputBar)
│   └── reminder-popup.tsx        # 提醒弹窗 + TTS + AI 对话处理
│
├── components/
│   ├── TaskCard.tsx               # 任务卡片 (2px 边框 + monospace 字体)
│   ├── InputBar.tsx               # 输入栏 (文字 ⇄ 语音切换，Haptics)
│   ├── ConfirmSheet.tsx           # AI 解析结果确认 Bottom Sheet
│   ├── ChatSheet.tsx              # AI 对话面板 (Bottom Sheet + BlurView + Spring)
│   ├── Toast.tsx                  # 轻量反馈提示 (ref 回调机制)
│   └── ErrorBoundary.tsx          # 全局错误捕获 + 重试按钮
│
├── services/
│   ├── ai.ts                     # DeepSeek API (8 个函数)
│   ├── ai-types.ts               # AIAction 联合类型 + AGENT_SYSTEM_PROMPT
│   ├── ai-config.ts              # API Key + Model 存储 (AsyncStorage)
│   ├── storage.ts                # 任务 CRUD + 批量操作 + 持久化
│   ├── notification.ts           # 系统通知 日程提醒 (expo-notifications)
│   ├── speech.ts                 # TTS 播报 + 预录音频播放 (expo-audio)
│   ├── ocr.ts                    # DeepSeek Vision OCR (强制 v4-flash)
│   ├── theme-context.tsx         # 主题上下文 (12 主题 + 系统/手动暗色)
│   └── theme-config.ts           # 主题持久化 (AsyncStorage)
│
├── constants/
│   ├── index.ts                  # COLORS (黑白像素风) + 分类/标签/模型
│   └── themes.ts                 # 12 套像素风主题定义
│
├── types/
│   └── index.ts                  # Task, AIParseResult, ReminderAction 等类型
│
├── assets/
│   ├── icon.png                  # APP 桌面图标 (蒙娜丽莎像素风, 1024px)
│   ├── splash-icon.png           # 启动画面
│   ├── android-icon-foreground.png  # Android 自适应图标前景
│   ├── android-icon-background.png  # Android 自适应图标背景
│   ├── favicon.png               # Web Favicon
│   ├── mona-avatar.png           # AI 头像 (蒙娜丽莎像素风)
│   └── audio/
│       └── reminder.wav          # 提醒音效 (占位)
│
├── app.json                      # Expo 配置 (权限/插件/splash)
├── eas.json                      # EAS 构建配置 (preview APK)
├── package.json                  # NPM 依赖 + 脚本
├── package-lock.json             # 锁定版本
└── tsconfig.json                 # TypeScript 配置
```

---

## 4. UI 设计 — 黑白像素艺术风格

### 设计哲学

- **极简黑白**：纯 `#000000` 与 `#FFFFFF` 双色调，无灰色过渡
- **像素风格**：2px 实线边框，`monospace` 等宽字体，符号化图标
- **高对比度**：黑色文字在白色背景上，白色文字在黑色按钮上
- **无圆角**：直角边框，像素艺术的标志性特征
- **无模糊阴影**：像素艺术不依赖光影，用边框区分层次

### 关键 UI 元素详解

#### 输入栏 (InputBar) — 三种模式互斥切换

```
┌──────────────────────────────────────────┐
│                     文字模式 (默认)        │
│ ┌──┐ ┌──┐ ┌──────────────────┐ ┌──────┐ │
│ │[]│ │(o)│ │> NEW TASK_       │ │█SEND█│ │
│ └──┘ └──┘ └──────────────────┘ └──────┘ │
│ 📷相机  🎤麦克风    输入框       发送按钮  │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│                     语音模式              │
│ ┌──┐        ┌────────┐                   │
│ │[K]│       │█HOLD▓█ │    SPEAK_         │
│ └──┘        └────────┘                   │
│ 切回键盘      按住说话按钮   提示文字       │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│                     录音模式              │
│        ┌──────────────┐                   │
│        │    ┌────┐    │                  │
│        │    │REC▓│    │  >>RELEASE<<     │
│        │    └────┘    │                  │
│        └──────────────┘                   │
│         声波脉冲动画       松开发送提示     │
└──────────────────────────────────────────┘
```

**切换交互：**
- 点 `(o)` → 旋转 180° 变 `[K]` → 进入语音模式
- 点 `[K]` → 变回 `(o)` → 进入文字模式
- 每次切换触发 `Haptics.ImpactFeedbackStyle.Light` 轻震
- 按住 HOLD → `Medium` 中震
- 上滑超过 60px → `Warning` 警告震动 → 变红 → 松手取消

#### 任务卡片 (TaskCard)

```
┌──────────────────────────────────────┐
│ !!  14:00   项目评审会议          >> │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│ ··  09:30   买菜                  >> │
└──────────────────────────────────────┘

优先级图标: !! 高  ·· 中  -- 低
时间格式: HH:MM (24小时)
>>: 重复任务标记
点击 → ChatSheet (任务上下文)
长按 → Alert 菜单 (编辑/删除)
```

#### AI 对话面板 (ChatSheet)

```
┌──────────────────────────────────────────┐
│  ════════  拖拽把手  ════════            │ ← 下拉关闭
│  ┌────┐                                  │
│  │蒙娜│  AI 助手                           │ ← 40×40 头像
│  │丽莎│  在线                              │
│  └────┘                                  │
│ ════════════════════════════════════════ │
│                                          │
│  ┌─────────────────────────────────┐    │
│  │ 蒙│ 你好呀！我是你的AI提醒助手    │    │ ← AI 气泡
│  │娜│                               │    │
│  └─────────────────────────────────┘    │
│                ┌──────────────────┐       │
│                │ 明天下午3点开会    │ 用户 │ ← 用户气泡
│                └──────────────────┘       │
│                                          │
├──────────────────────────────────────────┤
│ 背景: BlurView intensity=25 + 暗化遮罩    │
├──────────────────────────────────────────┤
│ ┌──┐ ┌──┐ ┌──────────────────┐ ┌──────┐ │
│ │[]│ │(o)│ │>                │ │█SEND█│ │
│ └──┘ └──┘ └──────────────────┘ └──────┘ │
└──────────────────────────────────────────┘

动效参数:
  打开: Spring(Stiffness 160, Damping 22, Mass 1)
  关闭: Spring(Stiffness 200, Damping 26, Mass 1)
  背景: Opacity 0→1 (300ms) + Blur 0→25 (同步)
  高度: 屏幕高度 × 75%
```

#### 底部导航栏 (Tab Bar)

```
┌─────────────┬─────────────┬─────────────┐
│             │             │             │
│   ● 今日    │   ≡ 全部    │   ⚙ 设置    │
│   (激活)    │             │             │
│             │             │             │
└─────────────┴─────────────┴─────────────┘

边框: 2px 实线 (四边)
图标: Feather (check-circle / list / settings)
激活态: 黑色背景 + 白色文字
非激活态: 白色背景 + 灰色文字
标签: monospace 字体, 10px, 700 字重
```

---

## 5. 用户交互流程

### 5.1 创建任务

```
用户输入 "明天下午3点开会讨论项目进度"
    ↓
点 SEND
    ↓
getApiKey() → 有 Key → 放行
    ↓
multiIntent(text) → callDeepSeek(messages, jsonMode=true)
    ↓
AI 返回: {"actions":[{"action":"create","title":"开会讨论项目进度",
          "datetime":"2026-06-07T15:00:00","priority":"中","category":"工作"}]}
    ↓
executeActions → case 'create'
  → setAiResult(parsed)
  → setShowConfirm(true)
    ↓
ConfirmSheet 弹出确认卡片
┌──────────────────────────────────┐
│        AI 理解结果                │
│                                  │
│  📋 开会讨论项目进度              │
│  ⏰ 2026年6月7日 15:00           │
│  🔥 中                           │
│  🔁 不重复                       │
│  📁 [工作]                       │
│                                  │
│   [修改]    [✓ 确认创建]         │
│           [取消]                  │
└──────────────────────────────────┘
    ↓
确认 → addTask() → scheduleTaskReminder() → 异步保存
    ↓
toast "「开会讨论项目进度」已创建" ✓ (3.5秒后消失)
```

### 5.2 完成/删除/修改任务（首页指令）

```
用户输入 "完成买菜"
    ↓
multiIntent → callDeepSeek
  → {"actions":[{"action":"complete","taskId":"xxx","summary":"「买菜」已完成"}]}
    ↓
executeActions → case 'complete'
  → toggleComplete(taskId) → 设置 completedAt
  → Alert.alert("「买菜」已完成 ✓")
    ↓
refresh() → 任务从列表消失 (移入"已完成"tab)

用户输入 "删除所有低优先级的任务"
    ↓
multiIntent → 返回 delete action (filter: 'low_priority')
    ↓
executeActions → case 'delete'
  → 遍历 tasks → filter → deleteTask → cancelTaskReminder
  → Alert.alert("已删除 3 个任务")
  → refresh()
```

### 5.3 语音输入（mock STT）

```
点击 (o) → 切换到语音模式
    ↓
按住 HOLD 按钮
  → Haptics Medium 震动
  → 声波动效启动 (pulseAnim + ringAnim)
  → 提示文字 ">>RELEASE<<"
    ↓
    ├── 松手 → stopRecording()
    │   → 如果不是取消 → onVoiceResult("下周二下午三点开会讨论项目进度")
    │   → handleSendText(mockResult) → 走 5.1 创建流程
    │
    └── 上滑超过 60px
        → Haptics Warning 连续振动
        → 提示文字变红 "CANCEL!"
        → 松手 → 取消录音 → 不触发任何操作
```

### 5.4 任务上下文 AI 对话

```
全部任务页 → 点击任务卡片 "项目评审会议"
    ↓
ChatSheet 从底部弹簧浮出 (Stiffness 160, Damping 22)
    ├── 标题栏: 蒙娜丽莎头像 + "编辑：项目评审会议"
    ├── AI 开场: "好的，我们来看看「项目评审会议」。
    │             你可以直接说"改到明天下午"、"标记为完成"、"加重要"等。"
    └── InputBar 启用 (隐藏相机按钮 showCamera=false)
    ↓
用户输入 "改到后天上午10点，优先级改高"
    ↓
chat() → callDeepSeek → AI 返回 reply + actions
    ↓
executeActions → edit action
  → updateTask(id, { datetime: '...', priority: '高' })
  → cancelTaskReminder + scheduleTaskReminder (新时间)
    ↓
refreshTasks() → 列表自动刷新
关闭 ChatSheet → onClose() → 父组件 refresh()
```

### 5.5 拍照 OCR 创建任务

```
首页 📷 按钮 → /new-task/camera
    ↓
选择 [📸 拍照] 或 [🖼️ 相册]
    ↓
expo-image-picker 获取图片 + base64
    ↓
recognizeText(base64)
  → deepseekVisionOCR (模型: deepseek-v4-flash, 无 temperature)
  → POST https://api.deepseek.com/chat/completions
  → 返回识别文字
    ↓
显示 OCR 结果 → 用户点 [确认并设置时间]
    ↓
跳转 /new-task/confirm → 手动编辑确认
  → 标题/时间/优先级/分类/重复/备注 全部可调
    ↓
[✓ 保存任务] → addTask + scheduleTaskReminder
```

### 5.6 全部任务页 (待办/已完成 分段)

```
┌──────────────────────────────────┐
│ ● 待办 (3)      ✓ 已完成 (5)    │ ← 分段切换 (segmented control)
└──────────────────────────────────┘
┌──────────────────────────────────┐
│ 全部  工作  学习  健康  生活     │ ← 分类筛选 (仅在"待办"tab 显示)
└──────────────────────────────────┘

待办视图: TaskCard 列表 (可点击 → ChatSheet)
已完成视图: ✓ 绿色对勾 + 任务名(删除线) + 完成日期
```

### 5.7 提醒弹出处理

```
系统通知触发 (scheduleTaskReminder → SchedulableTriggerInputTypes.DATE)
    ↓
reminder-popup.tsx 渲染
    ↓
useEffect 立即执行:
  → speakReminder("提醒：" + title) (TTS 播报)
  → playReminderSound() (expo-audio 预录音频)
  → BackHandler 阻止硬件返回键
    ↓
用户操作选择:
  ├── [延后5分钟] → handleSnooze(5) → 改 datetime → 重新 schedule
  ├── [✓ 已完成] → toggleComplete → cancelTaskReminder → dismiss
  └── AI 对话输入 "推迟1小时" →
       handleReminderResponse() → AI 解析
       ├── snooze → handleSnooze(60)
       ├── complete → toggleComplete + dismiss
       └── reschedule → updateTask(newDatetime) + 重新 schedule
```

### 5.8 设置页

```
┌──────────────────────────────────┐
│ 🎨 主题                           │
│ [像素黑白] [暗黑] [新闻] [网格]    │
│ [绿屏] [琥珀]                      │
├──────────────────────────────────┤
│ 🌙 外观                           │
│ [跟随系统] [浅色] [深色]          │
├──────────────────────────────────┤
│ 🤖 AI 配置                        │
│ API Key: [sk-****]              │
│ [保存] [清除]                    │
│ 模型: [V3 Chat] [Reasoner]      │
│       [V4 Flash] [V4 Pro]       │
├──────────────────────────────────┤
│ 🔊 提醒 · 系统 TTS 播报           │
│ 📷 OCR · DeepSeek Vision         │
│ ⚡ 性能 · [减少动画效果] 开关      │
├──────────────────────────────────┤
│       [清除所有数据]              │
└──────────────────────────────────┘
```

---

## 6. API 服务详解

### DeepSeek AI 函数清单

| 函数 | 文件 | 用途 | JSON Mode | 返回类型 |
|------|------|------|:---:|------|
| `multiIntent()` | `ai.ts` | 首页输入 → 多功能识别 | 是 | `AIAction[]` |
| `parseTask()` | `ai.ts` | 创建任务解析 | 是 | `{parsed, rawTitle}` |
| `editTask()` | `ai.ts` | 修改任务 | 是 | `{changes, summary}` |
| `chat()` | `ai.ts` | 对话式 AI | 是 | `AIChatResponse` |
| `generateBriefing()` | `ai.ts` | 每日简报 | 否 | `string` |
| `suggestTasks()` | `ai.ts` | 智能建议 | 否 | `string` |
| `handleReminderResponse()` | `ai.ts` | 提醒响应解析 | 是 | `ReminderAction` |
| `ocrEnhance()` | `ai.ts` | OCR 文本增强 | 是 | `{cleaned, tasks}` |
| `deepseekVisionOCR()` | `ocr.ts` | 图片 OCR 识别 | 否 | `OCRResult` |

### AIAction 联合类型（`ai-types.ts`）

```typescript
type AIAction =
  | { action: 'create'; title: string; datetime?: string; priority?: Priority; ... }
  | { action: 'edit'; taskId?: string; changes: Partial<Task>; summary: string }
  | { action: 'delete'; taskId?: string; filter?: string; summary: string }
  | { action: 'complete'; taskId?: string; filter?: string; summary: string }
  | { action: 'snooze'; snoozeMinutes: number; message: string }
  | { action: 'reschedule'; newDatetime: string; message: string }
  | { action: 'query'; summary: string }
  | { action: 'batch'; operations: AIAction[]; summary: string }
  | { action: 'help'; message: string }
  | { action: 'unknown'; message: string };
```

### API 配置

| 参数 | 值 | 说明 |
|------|-----|------|
| 端点 | `https://api.deepseek.com/chat/completions` | DeepSeek API |
| API Key | 用户配置 → AsyncStorage `@snapspeak_ai_key` | 设置页录入 |
| 默认模型 | `deepseek-chat` | AsyncStorage `@snapspeak_ai_model` |
| OCR 模型 | `deepseek-v4-flash` (硬编码) | Vision 模型 |
| 温度 | 0.1 | 低随机性 |
| Max Tokens | 2048 | 足够长的返回 |
| JSON Mode | 移除 (v0.4.0) | 改用 prompt 指令要求纯 JSON |

### API 调用流程

```
callDeepSeek(messages, jsonMode)
  → getApiKey() → 无 key → throw Error("请配置 API Key")
  → getModel() → 默认 deepseek-chat
  → fetch(POST /chat/completions, { model, messages, temperature, max_tokens })
  → res.ok? 
    → YES → data.choices[0].message.content → return
    → NO  → throw Error("API 400: ...")
```

**错误处理链：**
```
callDeepSeek throw
  → multiIntent 不 catch → 往上抛
    → handleSendText catch → Alert.alert("处理失败", error.message)
    → finally → setSending(false)
```

---

## 7. 数据模型

### Task 类型

```typescript
interface Task {
  id: string;           // Date.now().toString(36) + Math.random()
  title: string;        // 任务标题
  description: string;  // 备注 (默认 '')
  datetime: string;     // ISO 格式提醒时间 (新建默认 +1h)
  priority: '高' | '中' | '低';  // 默认 '中'
  completed: boolean;   // 是否完成
  completedAt?: string; // 完成时间 ISO (完成时记录)
  recurring: RecurringRule;  // 重复规则
  category: Category;   // 分类
  source: 'voice' | 'photo' | 'text';  // 来源
  createdAt: string;    // 创建时间 ISO
}
```

### 关联类型

```typescript
type Category = '工作' | '学习' | '健康' | '生活' | '其他';
type Priority = '高' | '中' | '低';
type RecurringRule = 'none' | '每天' | '每周' | '每月' | '工作日' | '每两周';
type TaskInput = Omit<Task, 'id' | 'createdAt'>;

type AIParseResult = {
  title: string;
  datetime: string | null;
  priority: Priority;
  recurring: RecurringRule;
  category: Category;
  notes: string;
};

type ReminderAction = {
  action: 'snooze' | 'complete' | 'reschedule' | 'dismiss';
  snoozeMinutes: number | null;
  newDatetime: string | null;
  message: string;
};
```

### 存储函数

| 函数 | 签名 | 说明 |
|------|------|------|
| `loadTasks()` | `() => Promise<Task[]>` | 从 AsyncStorage 读取 |
| `saveTasks()` | `(tasks: Task[]) => Promise<void>` | 批量保存 |
| `addTask()` | `(input: TaskInput) => Promise<Task>` | 创建 + 自动生成 ID |
| `updateTask()` | `(id: string, updates: Partial<Task>) => Promise<Task \| null>` | 部分更新 |
| `deleteTask()` | `(id: string) => Promise<boolean>` | 删除，返回是否成功 |
| `toggleComplete()` | `(id: string) => Promise<Task \| null>` | 切换完成 + 记录 completedAt |
| `findTaskByTitle()` | `(title: string) => Promise<Task \| null>` | 标题模糊搜索 |
| `batchComplete()` | `(filter: string) => Promise<number>` | 批量完成，返回完成数 |
| `getTasksByDate()` | `(date: string) => Promise<Task[]>` | 按日期过滤 |

---

## 8. 构建与部署

### 构建流程

```
[本地开发]
  npx expo start --web     → Web 预览 (localhost:8081)
  npx expo start --lan     → Expo Go 局域网扫码

[代码提交]
  git add → git commit → git push

[云端构建 EAS]
  npx eas-cli build -p android --profile preview
    ↓
  EAS Cloud (Ubuntu 26.04 + JDK 17 + NDK 27b)
    → npm ci
    → expo prebuild (生成 android/ 原生项目)
    → gradlew assembleRelease
    → APK 上传 → Artifact URL
    ↓
  curl 下载 APK → GitHub Release
```

### EAS 构建配置

```json
// eas.json
{
  "build": {
    "preview": {
      "android": {
        "buildType": "apk",
        "withoutCredentials": true
      }
    }
  }
}
```

### Expo 配置要点

```json
// app.json 关键字段
{
  "expo": {
    "name": "语拍提醒",
    "slug": "snapspeak",
    "scheme": "snapspeak",
    "android": {
      "package": "com.snapspeak.app",
      "permissions": ["CAMERA","RECORD_AUDIO","POST_NOTIFICATIONS",
                       "SCHEDULE_EXACT_ALARM","READ_MEDIA_IMAGES"]
    },
    "plugins": [
      "expo-router",
      "expo-camera",
      ["expo-notifications", { "icon": "...", "color": "#000000" }],
      "expo-audio",
      ["expo-build-properties", { "android": { "minSdkVersion": 24 } }],
      "expo-font",
      "expo-asset"
    ]
  }
}
```

### 版本号管理

| 位置 | 字段 | 当前值 |
|------|------|:---:|
| `app.json` | `"version"` | 0.4.1 |
| `package.json` | `"version"` | 0.4.1 |
| Git Tag | `git tag` | v0.4.1 |
| EAS Build | `appBuildVersion` | 自动递增 |

### 构建排错记录

| 版本 | 错误 | 根因 | 修复 |
|:--:|------|------|------|
| v0.1.0 | 闪退 | 缺少 `expo-linking` + `safe-area-context` | `npx expo install` |
| v0.2.0 | 闪退 | `expo-av` SDK 56 不兼容 | 替换为 `expo-audio` |
| v0.3.0 | 构建失败 | `compileSdkVersion: 34` 太低 | 删除覆盖，让 Expo 自动选择 36 |
| 构建失败 | `minSdkVersion: 23` 与 Hermes 冲突 | 改为 24 |
| v0.4.0 | API 400 | `response_format: json_object` 不兼容 | 移除，改用 prompt 指令 |
| v0.4.0 | OCR 400 | `deepseek-chat` 不支持 Vision | 硬编码 `deepseek-v4-flash` |
| 点击无响应 | toast 2 秒闪消失 | 改为 `Alert.alert` 系统弹窗 |

---

## 9. 主题系统

### 主题定义接口

```typescript
interface ThemeColors {
  primary: string; primaryLight: string; bg: string; card: string;
  text: string; textLight: string; textMuted: string; border: string;
  danger: string; success: string; warning: string;
  chatBubbleAI: string; chatBubbleUser: string;
  chatBubbleAIText: string; chatBubbleUserText: string;
  headerBg: string; headerText: string;
  tabBarActive: string; inputBg: string;
  gradientStart: string; gradientEnd: string;
}
```

### 12 套主题详情

#### 亮色系列

| ID | 名称 | primary | bg | text | border |
|----|------|---------|------|------|--------|
| `pixel` | 像素黑白 | `#000` | `#FFF` | `#000` | `#000` |
| `pixel-news` | 像素新闻 | `#000` | `#F8F6F0` | `#111` | `#000` |
| `pixel-grid` | 像素网格 | `#000` | `#FFF` | `#000` | `#000` |
| `pixel-amber` | 像素琥珀 | `#000` | `#FFFAF0` | `#111` | `#000` |

#### 暗色系列

| ID | 名称 | primary | bg | text | border |
|----|------|---------|------|------|--------|
| `pixel-dark` | 像素暗黑 | `#FFF` | `#000` | `#FFF` | `#FFF` |
| `pixel-green` | 像素绿屏 | `#0F0` | `#001100` | `#0F0` | `#0F0` |

### 暗色模式逻辑

```typescript
// services/theme-context.tsx
function resolveThemeId(baseId, darkMode, systemIsDark): string {
  if (darkMode === 'dark')  return `${baseId}-dark`;
  if (darkMode === 'light') return baseId.replace('-dark', '');
  if (darkMode === 'system') return systemIsDark ? `${baseId}-dark` : baseId;
  return baseId;
}
```

**切换方式：**
- 设置页 → `🌙 外观` → `[跟随系统] [浅色] [深色]`
- 键盘 → `useColorScheme()` 实时监测
- 持久化 → AsyncStorage `@snapspeak_dark_mode`

---

## 10. 开发指南

### 环境准备

```bash
# 克隆仓库
git clone https://github.com/laohuhenmang-gif/snapspeak.git
cd SnapSpeak

# 安装依赖
npm install

# 启动开发服务器
npx expo start --web     # Web 浏览器预览
npx expo start --lan     # 局域网 (Expo Go 扫码)

# TypeScript 编译检查
npx tsc --noEmit

# 生产构建验证
npx expo export --platform web

# 清除缓存
npx expo start --clear

# 本地 APK 构建 (需 Android SDK 30+)
npx expo run:android --variant release

# 云端构建
npx eas-cli build -p android --profile preview
```

### 项目架构决策记录

| 日期 | 决策 | 原因 |
|------|------|------|
| 初始 | DeepSeek 选择 | 统一 Vision + Chat API，用户已有 Key |
| v0.2.0 | `expo-av` → `expo-audio` | SDK 56 不兼容，被标记 Unmaintained |
| v0.2.0 | 移除 `expo-linear-gradient` | Web 点击被梯度层拦截 |
| v0.3.0 | `/chat` 路由 → `ChatSheet` | 用户要求对话内嵌，非独立窗口 |
| v0.3.0 | 回退 `index.tsx` 布局 | `ListHeaderComponent` 导致 InputBar 不可点击 |
| v0.4.0 | 移除 `response_format` | `v4-flash` 兼容性，改用 Prompt 指令 |
| v0.4.0 | 黑白像素风 | 用户指定设计方向 |
| v0.4.0 | `minSdkVersion: 24` | Hermes 工具链要求 ≥24 |
| v0.4.0 | `compileSdkVersion` 删除 | 让 Expo SDK 自动选择 36 |

### 常见问题

**Q: 点击发送没有反应？**
A: 检查 API Key 是否配置（设置页）；确认网络可达 `api.deepseek.com`。失败会弹出 `Alert` 弹窗显示错误信息。

**Q: OCR 识别失败？**
A: OCR 强制使用 `deepseek-v4-flash` 模型，不受用户模型选择影响。检查图片大小（建议 < 10MB）。

**Q: 语音输入是假的吗？**
A: 当前阶段为 mock STT，返回固定文本用于演示流程。真实 STT 待集成 `@react-native-voice/voice`。

**Q: 如何添加蒙娜丽莎头像？**
A: 将 `mona-avatar.png` (建议 200×200px) 放入 `assets/` 目录，ChatSheet 中的 🤖 占位符自动替换为图片。

**Q: APP 图标如何更换？**
A: 替换 `assets/icon.png` (1024×1024) 后重新 EAS 构建。

---

## 11. 版本历史

| 版本 | Git Tag | 日期 | 要点 |
|:----:|---------|------|------|
| v0.1.0 | `v0.1.0` | 2026-06-06 | 首个 APK 构建，基础任务管理功能 |
| v0.1.1 | `v0.1.1` | 2026-06-06 | 添加 `expo-linking` + `safe-area-context` 修复闪退 |
| v0.2.0 | `v0.2.0` | 2026-06-06 | AI 机器人 multiIntent，待办/已完成分段，Toast 组件，Dribbble 紫色调 UI，`expo-av`→`expo-audio` |
| v0.3.0 | `v0.3.0` | 2026-06-06 | ChatSheet Bottom Sheet，暗色模式 12 主题，InputBar 模式切换，ErrorBoundary |
| v0.3.1 | `v0.3.1` | 2026-06-07 | 蒙娜丽莎头像 + APP 图标 |
| v0.4.0 | `v0.4.0` | 2026-06-07 | 黑白像素风主题，API 修复（去除 response_format，OCR 强制 vision 模型，Alert 弹窗） |
| v0.4.1 | `v0.4.1` | 2026-06-07 | 版本号同步（app.json + package.json: 1.0.0 → 0.4.1） |

---

## 附录

### GitHub 仓库

- **仓库地址**: https://github.com/laohuhenmang-gif/snapspeak
- **最新 Release**: https://github.com/laohuhenmang-gif/snapspeak/releases/latest
- **APP 显示名**: 语拍提醒
- **包名**: `com.snapspeak.app`

### APK 下载

| 版本 | 大小 | 下载链接 |
|:---:|:---:|------|
| v0.4.1 | 122 MB | [下载 APK](https://github.com/laohuhenmang-gif/snapspeak/releases/download/v0.4.1/snapspeak-v0.4.1.apk) |

### 联系

问题反馈请提交 [GitHub Issues](https://github.com/laohuhenmang-gif/snapspeak/issues)
