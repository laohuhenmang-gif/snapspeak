# SnapSpeak 全量代码审查报告

> 审查日期: 2026-06-07
> 审查范围: 所有源码文件 (types, services, components, app, constants)
> 对照标准: 《SnapSpeak AI个人助理改造修复升级方案》

---

## 一、全量问题清单 (按严重程度分级)

### 🔴 P0 - 致命问题 (导致无法使用)

| # | 问题 | 位置 | 详细说明 |
|---|------|------|----------|
| 1 | **语音是 mock** | `services/speech.ts` | 整个文件不是真实的语音识别。`startRecording`/`stopRecording` 只是模拟，Hooks 返回固定 mock 文本，不存在任何 Android SpeechRecognizer API 调用 |
| 2 | **OCR 不可用** | `services/ocr.ts` | OCR 只是调了 `fetch` 一个占位 endpoint，没有真实的 DeepSeek Vision API base64 图片传入逻辑，返回固定 mock 字符串 |
| 3 | **COLORS 全黑** | `constants/index.ts` | 所有颜色 (primary, danger, success, warning, border 等) 都是 `#000000`，导致按钮、文字、边框、优先级色块等完全同色，UI 一片漆黑无法辨认交互元素 |
| 4 | **主界面不是聊天式** | `app/(tabs)/index.tsx` | 当前是"今日任务列表 + FAB 聊天按钮"的 OA 式结构，不符合方案要求。缺少: 聊天流消息列表、AI 对话气泡、确认卡片、未完成原因记录、复盘卡片 |
| 5 | **ChatSheet 点击无反馈** | `components/ChatSheet.tsx` | ChatSheet 只是一个静态 popup 组件，没有 loading/thinking/error 等状态反馈。如果 API Key 未配置或网络失败，用户点击后无任何提示 |
| 6 | **InputBar 缺少相机入口** | `components/InputBar.tsx` | 原始版本只有语音和文字，没有相机按钮。无法从底部输入栏拍照或选图 |

### 🟡 P1 - 严重问题 (功能闭环缺失)

| # | 问题 | 位置 | 详细说明 |
|---|------|------|----------|
| 7 | **AI Action 无确认卡机制** | `app/(tabs)/index.tsx` | 当前 `executeActions` 对 create 动作直接弹出 ConfirmSheet 确认卡，但对 complete/delete/edit 等**直接执行**，不经用户确认。违反方案"用户确认后才执行"原则 |
| 8 | **缺少完整交互状态机** | 整个 App | 无 idle/listening/transcribing/sending/thinking/action_pending/executing/success/error 状态体系。用户看不到系统正在做什么 |
| 9 | **拍照流程不在聊天窗内** | 原 InputBar | 原始代码无 camera 按钮，拍照不在聊天窗口内完成，不符合"图片作为消息显示在聊天窗口"的要求 |
| 10 | **消息类型单一** | 整个 App | 只有文本消息，无 image/card/action/error 等多种消息类型。AI 确认卡、OCR 候选卡、复盘卡、错误重试卡都无法展示 |
| 11 | **缺少 AI 知识库** | 无此功能 | 方案要求的 5 层知识库 (偏好/背景/原因/复盘/规则) 完全不存在。AI 无法学习用户习惯 |
| 12 | **缺少复盘能力** | 无此功能 | 无每日复盘、完成率统计、未完成原因记录、AI 生成明日建议 |
| 13 | **缺少未完成原因记录** | 无此功能 | 用户说没完成时，AI 不能记录原因、生成新计划、提醒前置卡点 |
| 14 | **存储层是 AsyncStorage** | `services/storage.ts` | 用 AsyncStorage 存 JSON，不适合任务/知识库/对话等结构化数据。无 SQLite，无法高效查询和管理 |

### 🟢 P2 - 中等问题 (体验和可靠性)

| # | 问题 | 位置 | 详细说明 |
|---|------|------|----------|
| 15 | **notification 可靠性不足** | `services/notification.ts` | 未处理: App 被杀后提醒恢复、精确闹钟权限提示、电池优化限制提示、锁屏提醒专项处理 |
| 16 | **缺少错误状态反馈** | 多处 | API 失败只有 `console.warn`，无用户可见提示；网络失败无重试；OCR 失败无引导 |
| 17 | **独立捕捉页面 (new-task)** | `app/new-task/` | 有独立的 new-task 页面，违反方案"取消独立捕捉页面"的要求 |
| 18 | **页面过长** | `app/(tabs)/index.tsx` | 当前页面结构: 标题 + 设置banner + 简报卡片 + 建议卡片 + 任务列表 + FAB + ChatSheet + InputBar。太多层 |
| 19 | **主题文件颜色值不一致** | `constants/themes.ts` | 主题文件中的 primaryLight, chatBubbleUser 等属性与 constants/index.ts 中 COLORS 的字段不完全对应 |
| 20 | **AI 系统提示词未包含助理身份** | `services/ai-types.ts` | AI prompt 只是任务解析器，不包含: 友好语气、不责备原则、追问能力、知识库记录、复盘建议生成 |
| 21 | **ConfirmSheet 功能单一** | `components/ConfirmSheet.tsx` | 只能确认创建任务，不支持: 修改时间、逐条选择 OCR 候选、记录原因、调整计划、确认规则 |
| 22 | **toast 功能弱** | `components/Toast.tsx` | 全局 toast 缺少多级别 (success/info/warn/error)、缺少 loading 状态、不支持 undo |

---

## 二、已完成修复 (前五步实施)

| 步骤 | 文件 | 变更内容 |
|------|------|----------|
| ✅ 1 | `types/index.ts` | 新增 TaskSource 联合类型、ParseResult items 字段、AIAction 扩展 (blocker/review/query/multi_create)、Reminder 接口、Memory 接口、ConversationMessage 接口 |
| ✅ 2 | `services/ai-types.ts` | 升级 SYSTEM_PROMPT 为"个人 AI 工作助理"身份，包含友好语气、7 种核心能力、12 种 action 类型、确认卡模板、OCR 识别模板、复盘/原因记录模板 |
| ✅ 3 | `constants/interaction.ts` | 新增完整交互状态机 (11 种状态)、错误消息体系 (8 种场景)、InputMode 类型 |
| ✅ 4 | `constants/index.ts` | COLORS 从全黑改为有区分度的真实颜色值，新增 primaryLight/chatBubbleUser/chatBubbleUserText/chatBubbleAI/chatBubbleAIText 等 |
| ✅ 5 | `components/InputBar.tsx` | 重写为三入口组件 (语音/文字/相机)，支持 interactionState 驱动，有触觉反馈、声波动画、状态条、权限检查、拍照/相册选择 |
| ✅ 6 | `components/ChatBubble.tsx` | 全新组件，支持 5 种消息类型 (text/image/card/action/error)，确认卡支持多 action 按钮 (primary/secondary/danger)，错误重试 |

---

## 三、下一步实施计划

### 步骤 7: 重写主界面 (chat.tsx → 替换 index.tsx)

将 `app/(tabs)/index.tsx` 从"今日任务列表 + FAB"改为**聊天式单屏主界面**：

```
┌──────────────────────────────┐
│ 语拍提醒 / AI 助理             │
│ 在线 · 今天有 3 件重要事项       │
├──────────────────────────────┤
│ AI 气泡：早上好，今天我帮你盯住… │
│ 今日任务摘要卡                  │
│ 用户消息：明天下午提醒我…        │
│ AI 任务确认卡：是否创建？        │
│ AI 复盘卡：今天完成 4/6 件…      │
├──────────────────────────────┤
│ 🎙 输入任务…  📷 发送          │
└──────────────────────────────┘
```

核心变更:
- FlatList 渲染 ChatMessage 列表 (替代 TaskCard 列表)
- 使用 ChatBubble 组件渲染每条消息
- 底部 InputBar 绑定 interactionState
- 移除 ChatSheet FAB、移除独立捕捉页
- AI 回复生成确认卡 (ConfirmCardData) 而非直接执行

### 步骤 8: 重写 ConfirmSheet → 内嵌到确认卡

将确认逻辑从独立 modal 移入聊天流的 card/action 消息中，使用 ChatBubble 的 onCardAction 回调处理用户确认。

### 步骤 9: 接入真实语音识别

替换 `services/speech.ts` 中的 mock 实现，使用 `expo-speech-recognition` 或 Android 原生 SpeechRecognizer API。

### 步骤 10: 升级存储层到 SQLite

使用 `expo-sqlite` 创建 8 张表 (tasks/captures/reminders/ai_actions/memories/reflections/blockers/conversations)，迁移现有 AsyncStorage 数据。

### 步骤 11: 实现 AI 知识库

实现 5 层知识库的记忆存储、规律发现、用户确认、规则应用闭环。

### 步骤 12: 实现复盘与效率反馈

AI 每日/每周主动推送复盘卡片，统计完成率，分析未完成原因，生成明日计划建议。

---

## 四、架构改进总览

```
Before (原系统):
┌─────────┐  ┌─────────┐  ┌─────────┐
│ 捕捉页面  │  │ 今日任务  │  │ 全部任务  │
│ (独立)   │  │ (列表)   │  │ (列表)   │
└─────────┘  └─────────┘  └─────────┘
      ↓           ↓           ↓
   文字输入    AI确认弹窗   ChatSheet浮层
   
   问题: 页面多、入口散、AI弱、无闭环

After (改造后):
┌──────────────────────────────┐
│         今天 / AI 助理         │ ← 唯一主界面
│    ┌────────────────────┐   │
│    │   聊天消息流         │   │
│    │   · 文字/图片/卡片   │   │
│    │   · AI确认/复盘/建议 │   │
│    └────────────────────┘   │
│  🎙 输入...  📷 发送         │ ← 三合一输入栏
└──────────────────────────────┘
       ↓            ↓
   全部任务页      设置页
   (查看+搜索)    (API+主题+AI)