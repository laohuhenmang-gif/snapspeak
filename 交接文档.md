# SnapSpeak 项目交接文档

> **项目名称**：SnapSpeak / 语拍提醒  
> **定位**：安装在安卓手机里的个人 AI 工作助理  
> **交接日期**：2026-06-14  
> **当前版本**：v2.1.0  
> **GitHub**：https://github.com/laohuhenmang-gif/snapspeak

---

## 一、项目概述

SnapSpeak 是一款基于 React Native (Expo) 开发的安卓 AI 个人助理应用。用户通过文字、语音、拍照与 AI 沟通，让 AI 帮助记录事项、生成提醒、复盘完成情况、记录未完成原因并重新制定计划。

### 技术栈
| 技术 | 版本 |
|---|---|
| Expo SDK | 52 |
| React Native | 0.76.7 |
| React | 18.3.1 |
| TypeScript | 5.3.3 |
| SQLite | expo-sqlite 15.x |
| 目标平台 | Android (arm64-v8a) |

---

## 二、功能清单

### 核心功能
- 聊天式单屏主界面（类微信对话窗口）
- 三大输入：文字 / 语音识别 / 拍照 OCR
- AI 对话 + 10 种 Action 确认卡（创建/修改/删除/完成/延后/改期/记录原因/创建提醒/生成计划/复盘）
- SQLite 8 张表持久化
- 知识库记忆系统（5 层：偏好/任务背景/未完成原因/复盘/规则）
- 每日/每周复盘
- 通知提醒 + AlarmManager 精确提醒 + 开机恢复

### 体验功能
- 三 Tab 导航（今天/全部/设置）
- 10 套主题（含暗色）
- 4 个 AI 角色（小拍/小智/小护/教练）
- 消息入场动画
- 触觉反馈
- 消息长按菜单
- 任务左滑操作
- 图片消息缩略图
- 下拉刷新
- 空状态插画
- 启动屏
- 首次使用引导（Onboarding）

### AI 配置
- 聊天模型独立配置
- 视觉模型独立配置（OCR/图片理解）
- 一键配置按钮
- 多提供商支持（DeepSeek/OpenAI/Agnes/自定义）

### 安全与稳定性
- PIN 码应用锁
- 崩溃日志记录与导出
- Error Boundary 全局错误边界
- 应用状态恢复
- 离线请求队列
- 网络监控
- 数据库迁移管理
- 自动备份
- 数据保留策略

### 构建
- Release 签名配置
- ProGuard 代码混淆
- ABI 拆分（arm64-v8a）
- 版本号统一管理

---

## 三、目录结构

```
D:\DevEnv\snapspeak\
├── app/                          # 路由页面
│   ├── (tabs)/                   # Tab 导航组
│   │   ├── _layout.tsx           # Tab 布局（3 Tab）
│   │   ├── index.tsx             # 今天（聊天主界面）
│   │   ├── all.tsx               # 全部任务（FlatList + 搜索 + 筛选）
│   │   └── settings.tsx          # 设置（AI配置/外观/安全/数据）
│   ├── _layout.tsx               # 根布局（启动屏+应用锁+引导）
│   └── task/[id].tsx             # 任务详情
├── components/                   # UI 组件（20+）
│   ├── AnimatedMessage.tsx       # 消息入场动画
│   ├── AppLock.tsx               # PIN 码应用锁
│   ├── BottomInputBar.tsx        # 底部输入栏
│   ├── ConfirmCard.tsx           # AI Action 确认卡
│   ├── EmptyState.tsx            # 空状态
│   ├── ErrorBanner.tsx           # 错误提示
│   ├── ErrorBoundary.tsx         # 全局错误边界
│   ├── LazyLoad.tsx              # 懒加载
│   ├── MessageContextMenu.tsx    # 消息长按菜单
│   ├── Onboarding.tsx            # 首次使用引导
│   ├── PermissionGuide.tsx       # 权限引导
│   ├── PixelAvatar.tsx           # 像素头像
│   ├── PixelButton.tsx           # 按钮（圆角风格）
│   ├── PixelCard.tsx             # 卡片（圆角风格）
│   ├── PriorityBadge.tsx         # 优先级标签
│   ├── SplashScreen.tsx          # 启动屏
│   ├── StatusBanner.tsx          # 状态栏
│   ├── SwipeableTask.tsx         # 可滑动任务
│   └── TaskSummaryCard.tsx       # 今日任务摘要
├── services/                     # 服务层（15+）
│   ├── ai.ts                     # AI 对话（chat/review/plan）
│   ├── ai-config.ts              # AI 配置（模型/提供商/API Key）
│   ├── ai-types.ts               # AI 类型定义（10 种 Action）
│   ├── auto-backup.ts            # 自动备份
│   ├── crash-log.ts              # 崩溃日志
│   ├── data-retention.ts         # 数据保留策略
│   ├── db-migration.ts           # 数据库迁移
│   ├── haptics.ts                # 触觉反馈
│   ├── http-client.ts            # HTTP 请求（超时+重试）
│   ├── image-compress.ts         # 图片压缩
│   ├── native-reminder.ts        # 原生提醒桥接
│   ├── network.ts                # 网络监控
│   ├── notification.ts           # 通知提醒
│   ├── ocr.ts                    # OCR 识别（视觉模型）
│   ├── offline-queue.ts          # 离线请求队列
│   ├── reminder-service.ts       # 提醒语音播报
│   ├── speech.ts                 # 语音识别（设备端 STT）
│   ├── state-recovery.ts         # 应用状态恢复
│   ├── storage.ts                # SQLite 数据库（8 张表）
│   ├── theme-config.ts           # 主题配置持久化
│   └── theme-context.tsx         # 主题 Context
├── constants/                    # 常量
│   ├── interaction.ts            # 错误类型
│   ├── personas.ts               # AI 角色配置（4 个）
│   └── themes.ts                 # 主题颜色（10 套）
├── types/
│   └── index.ts                  # TypeScript 类型定义
├── android/                      # Android 原生代码
│   └── app/src/main/java/com/snapspeak/app/
│       ├── AlarmHelper.kt        # AlarmManager 封装
│       ├── AlarmReceiver.kt      # 闹钟触发接收器
│       ├── BootReceiver.kt       # 开机恢复接收器
│       ├── NativeReminderModule.kt  # RN 桥接模块
│       ├── NativeReminderPackage.kt # 模块注册
│       └── PermissionHelper.kt   # 权限工具类
├── scripts/
│   └── build-release.sh          # 构建脚本
├── releases/                     # 版本归档
│   ├── apk/                      # APK 文件
│   │   └── SnapSpeak-v2.1.0-release.apk
│   ├── v1.0.0/ ~ v2.1.0/        # 各版本源码快照
│   └── ...
├── app.json                      # Expo 配置
├── package.json                  # 依赖定义
├── tsconfig.json                 # TypeScript 配置
└── babel.config.js               # Babel 配置
```

---

## 四、数据库结构（8 张表）

| 表名 | 用途 | 关键字段 |
|---|---|---|
| tasks | 任务 | id, title, planned_at, priority, status, category, recurring, completed, postponed_count, current_blocker_reason, needs_precheck |
| captures | 捕捉记录 | id, type(voice/text/photo), raw_text, image_uri, ocr_text |
| reminders | 提醒 | id, task_id, remind_at, remind_type, notification_id |
| ai_actions | AI 动作 | id, action_type, payload_json, status(pending/confirmed/executed/cancelled/failed) |
| memories | 知识库 | id, memory_type(preference/blocker/rule/project/habit), content, confidence, user_confirmed |
| reflections | 复盘 | id, date, planned_count, completed_count, completion_rate, ai_summary, ai_suggestion |
| blockers | 卡点 | id, task_id, blocker_type, blocker_text, suggested_action, resolved |
| conversations | 对话 | id, role, content, message_type, related_task_id |

---

## 五、AI 功能架构

### Action 类型（10 种）
```
create / edit / delete / complete / snooze / reschedule
record_blocker / create_reminder / create_memory / create_plan / generate_review
```

### 确认流程
```
用户输入 → AI 理解 → 生成 Action → 确认卡展示 → 用户确认 → 执行 → 反馈
```

### 知识库闭环
```
捕捉 → 理解 → 生成建议 → 用户确认 → 执行动作 → 提醒跟踪
→ 询问原因 → 复盘 → 知识库更新 → 新计划
```

---

## 六、构建与发布

### 构建命令
```bash
# 开发模式
npm start

# 构建 Release APK
npm run build:release

# 或手动
cd android
./gradlew assembleRelease -PversionCode=210 -PversionName="2.1.0"
```

### 版本号管理
```bash
npm run version:patch   # 2.1.0 → 2.1.1
npm run version:minor   # 2.1.0 → 2.2.0
npm run version:major   # 2.1.0 → 3.0.0
```

### APK 输出位置
```
android/app/build/outputs/apk/release/app-release.apk
```

### GitHub 发布
```bash
# 创建 Tag
git tag -a v2.1.0 -m "SnapSpeak v2.1.0"
git push origin v2.1.0

# 创建 Release（需要 gh CLI 认证）
gh release create v2.1.0 --title "SnapSpeak v2.1.0" --notes "..." releases/apk/SnapSpeak-v2.1.0-release.apk
```

---

## 七、开发注意事项

### Expo 版本兼容性
项目使用 Expo SDK 52，新增 expo-* 包时必须安装兼容版本：
- `expo-document-picker@~12.0.0`（非 56.x）
- `expo-file-system@~17.0.0`（非 56.x）
- `expo-haptics@~13.0.0`（非 56.x）
- `expo-image-manipulator@~12.0.0`（非 56.x）

### SecureStore 兼容性
Android 上 SecureStore 可能失败，`setApiKey()` 已做降级处理（try-catch → AsyncStorage）。修改 ai-config.ts 时保持此降级逻辑。

### 原生模块
自定义原生模块（AlarmHelper/BootReceiver/NativeReminderModule）位于 `android/app/src/main/java/com/snapspeak/app/`，修改后需重新构建。

### TypeScript 检查
每次修改后运行：
```bash
node node_modules/typescript/bin/tsc --noEmit
```

### Bundle 验证
```bash
node node_modules/expo/bin/cli export --platform android --output-dir dist-check
```

---

## 八、已知问题与待优化

| 优先级 | 问题 | 说明 |
|---|---|---|
| P2 | 每周复盘交互按钮 | 复盘消息可操作但 UI 可进一步优化 |
| P2 | 数据导入 UI | importAllData 函数已有，可考虑更友好的导入界面 |
| P3 | 更多主题 | 当前 10 套，可继续扩展 |
| P3 | 历史搜索增强 | 当前支持基本搜索，可增加高级筛选 |
| P3 | 云端 STT 选项 | 当前语音使用设备端，可选配云端 STT |

---

## 九、验收标准

### 必须通过
- [x] 文字能创建任务
- [x] 语音能真实识别并创建任务
- [x] 拍照能识别文字并生成任务候选
- [x] AI 能生成确认卡
- [x] 用户确认后才执行动作
- [x] 任务能保存
- [x] 提醒能触发（含锁屏/后台/重启）
- [x] 任务能完成/修改/查询
- [x] API Key 保存正常
- [x] 引导页流程正常
- [x] 图片消息显示缩略图
- [x] 对话历史重启保留

---

## 十、联系信息

- **GitHub**：https://github.com/laohuhenmang-gif/snapspeak
- **项目路径**：`D:\DevEnv\snapspeak`
- **版本归档**：`releases/` 目录下各版本文件夹
